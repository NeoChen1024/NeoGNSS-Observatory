cmake_minimum_required(VERSION 3.24)
project(NeoGNSSObservatory LANGUAGES CXX)
include(CTest)
add_subdirectory(libcppubx2)
option(NEOGNSS_BUILD_RINEX_TOOLS "Build experimental tools using the pinned RTKLIB-EX" OFF)
if(NEOGNSS_BUILD_RINEX_TOOLS)
    enable_language(C)
    add_subdirectory(native)
endif()
